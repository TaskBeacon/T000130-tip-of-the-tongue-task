# Task Plot Brief

Title: Tip-of-the-Tongue Elicitation Task
Construct: lexical retrieval / metamemory

Canonical sources inspected: main.py,config/config.yaml,src/run_trial.py,src/utils.py and native gate/semantic screenshots. Build5gatesPASS;14nativecases/21assertionsPASS before plotting.

One block12 original Chinese definitions; same procedure each item. Three representative response paths: Know,TOT,Don't know (missing judgment also goes straight to verification, but remains analytically distinct). Common start is self-paced ready(space) followed by fixation+(0.5s). Each row begins at judgment(20s maximum). Know: complete answer15s(F2)→verification10s→saved0.5s. TOT: initial sound10s(F2)→character count8s(1–4 or0 unknown)→related words10s(F2)→resolution15s(F2)→verification10s→saved0.5s. Don't know: verification10s→saved0.5s. F4 during ANY partial phase skips remaining partial phases and starts complete resolution entry. Verification2 opens optional alternative-target entry10s(F2) before saved. Target unseen before verification.

Representative actual stimulus: 古人利用太阳照出的指针影子，在刻有时刻的盘面上读出时间的器具。 Intended target日晷 appears only at verification. Native text input blank. Show editable boxes with example participant text only when clearly marked as an example response. Partial initial-sound box may show r; count2; resolution日晷 as example typed response, not an answer cue. No rewards, no correctness feedback. On-screen prompts remain Chinese; figure phase labels may be English.

Figure uses three horizontal paths. Common ready/fixation strip above rows and saved/alternative-branch annotation prevent overlong rows. Main TOT row has7snapshots. Timing labels are maxima for response windows, actual configured0.5s for saved. Gray screen boxes,white background,dark legible type,restrained accents,top18%blankreservedforbranding. No data/norms/results chart. Rendered native screenshots show no clipping/overlap. Generated final must be visually inspected; max5totalrounds authorized.
